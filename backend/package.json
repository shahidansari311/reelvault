{
  "name": "reelvault-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && (python3 -m pip install -U \"yt-dlp[default]\" || pip install -U \"yt-dlp[default]\" || echo 'Pip failed')",
    "postinstall": "python3 -m pip install -U --pre \"yt-dlp[default]\" || pip install -U --pre \"yt-dlp[default]\" || echo 'Skipping yt-dlp update'",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "instagram-url-direct": "^1.0.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
